import { Placeholder } from '@/components/placeholder';

export default function Day() {
  return (
    <Placeholder
      title="A day, closed"
      note="The verdict, the In short reading, training by muscle group with each exercise's delta, eating as macros and meals, and the body. WP6b builds this from GET /api/day/:date, which already returns all of it."
    />
  );
}
