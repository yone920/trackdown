import { Placeholder } from '@/components/placeholder';

export default function Progress() {
  return (
    <Placeholder
      title="Progress"
      note="One section per active goal, rendered from its metrics, then consistency and coverage. WP6b builds this from GET /api/goals/:id/progress."
    />
  );
}
