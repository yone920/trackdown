import { Placeholder } from '@/components/placeholder';

export default function Goals() {
  return (
    <Placeholder
      title="Goals"
      note="Active goals as cards with their progress rings, the history of past ones, and the account rows below them. WP6b builds this from GET /api/goals. Setting a goal already works: tell the + sheet what you are after."
    />
  );
}
