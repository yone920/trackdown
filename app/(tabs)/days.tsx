import { Placeholder } from '@/components/placeholder';

export default function Days() {
  return (
    <Placeholder
      title="Days"
      note="Every day you have logged, grouped by week, with the verdict against the goal that was active that day. WP6b builds this from GET /api/days."
    />
  );
}
